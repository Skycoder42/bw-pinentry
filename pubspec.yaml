name: bw_pinentry
description: A pinentry wrapper around the bitwarden CLI to use your vault for GPG-Key storage.
version: 1.0.0
repository: https://github.com/Skycoder42/bw-pinentry
publish_to: none

environment:
  sdk: ^3.7.0

platforms:
  linux:

dev_dependencies:
  custom_lint: ^0.7.3
  dart_pre_commit: ^5.4.3
  dart_test_tools: ^6.1.0
